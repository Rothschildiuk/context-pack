[package]
name = "context-pack"
version = "0.6.0"
edition = "2021"
description = "Compact repository context bundles for coding agents"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
